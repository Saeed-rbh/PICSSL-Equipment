# SMTP Configuration for Email Notifications
# You can use Gmail, Outlook, or any standard SMTP server.

# Example: Gmail (requires App Password)
# 1. Go to Google Account > Security > 2-Step Verification > App passwords
# 2. Select "Mail" and "Other (Custom name)" -> "OPTIR Website"
# 3. Copy the 16-character password

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Example: Ethereal (for testing - crates a fake inbox)
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=example_user
# SMTP_PASS=example_pass
